{
	"scripts": {
		"build": "vite build",
		"type-check": "tsc",
		"dev": "turbo dev --parallel",
		"clean": "rm -rf node_modules && turbo clean && rm -rf bun.lockb"
	},
	"dependencies": {
		"typescript": "^5.4.2"
	},
	"workspaces": [
		"apps/*",
		"packages/*"
	]
}
