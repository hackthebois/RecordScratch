{
  "scripts": {
    "build": "dotenv -- turbo build",
    "type-check": "tsc",
    "dev": "dotenv -- turbo dev",
    "clean": "rm -rf node_modules && turbo clean && rm -rf bun.lockb"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "typescript": "^5.4.2"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "dotenv-cli": "^7.4.1"
  }
}
