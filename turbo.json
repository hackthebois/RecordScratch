{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"build": {
			"outputs": ["dist/**"]
		},
		"type-check": {},
		"dev": {
			"persistent": true,
			"cache": false
		},
		"clean": {}
	},
	"globalEnv": ["DATABASE_URL"]
}
